server.port=5000

# MongoDB
spring.data.mongodb.uri=mongodb://localhost:27017/global_handlooms

# Mail (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# JWT
app.jwt.secret=my_super_secret_jwt_key_123_padded_to_256bits!
app.jwt.expiration=86400000
app.jwt.override-expiration=43200000

# Multipart (large image uploads)
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Logging
logging.level.com.globalhandlooms=INFO
logging.level.org.springframework.security=WARN
